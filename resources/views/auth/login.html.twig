{% extends "layout.html.twig" %}

{% block content %}

    <div class="container"><br>
        <div class="row">
            <div class="col-lg-4"></div>
            <div class="col-lg-4">
                <form action="{{ path('admin_login_check') }}" method="post">

                    <div class="invalid-feedback">
                        {{ error }}
                    </div>

                    <div class="form-group">
                        <label>Username</label>
                        <input class="form-control form-control-lg" type="text" name="_username" value="{{ last_username }}" />
                    </div>

                    <div class="form-group">
                        <label>Password</label>
                        <input class="form-control form-control-lg" type="password" name="_password" value="" />
                    </div>

                    <div class="form-group">
                        <input class="btn btn-primary btn-lg" type="submit" />
                    </div>
                </form>
                    <br><br>
            </div>
            <div class="col-lg-4"></div>
        </div>
    </div>
{% endblock %}